# GoogleFormQR大量生成ツール 質問事項

仕様書を読んで疑問に思った点をまとめました。各項目の下に回答を追記してください。

---

## 1. URLテンプレートの置換記法について

仕様書では `{CODE}` をプレースホルダとしていますが、Google Formの事前入力URL例（`entry.2096451574=GAME01`）では `=` 直後の値そのものが置換対象です。

- 質問1-1: ユーザーが入力するURLテンプレートの想定は以下のどちらですか?
  - **A**: ユーザーが手動で `{CODE}` を挿入する（例: `...entry.2096451574={CODE}`）
  - **B**: 既存の値（例: `GAME01`）が入ったURLを貼り付けたら、ツール側で値部分を自動検出して置換する
  - **C**: 両方サポート

> 回答:Aの想定です。

- 質問1-2: 複数のパラメータ（複数の `{CODE}`、または `{CODE1}` `{CODE2}` など）に対応する必要はありますか? MVPでは単一プレースホルダのみで良いですか?

> 回答:MVPでは単一プレースホルダのみで構いません。

---

## 2. 連番生成の仕様

- 質問2-1: 桁数指定時、開始番号や終了番号が桁数を超える場合の挙動はどうしますか?
  - 例: 桁数=2 で 終了=150 → `GAME150` のように桁あふれを許容? それともバリデーションエラー?

> 回答:バリデーションエラーで良いです。

- 質問2-2: ステップ（増分）の指定は必要ですか? （例: 2, 4, 6, …） MVPでは「1ずつ」のみで良いですか?

> 回答:MVPでは「1ずつ」のみで構いません。

- 質問2-3: Suffix（接尾辞）は必要ですか? 例: `GAME01_A` のような形式。

> 回答:MVPでは不要です。将来拡張として検討。

---

## 3. CSV入力の仕様

- 質問3-1: CSV入力は「1行1コード」のテキストエリア入力と、ファイルアップロードの両方ですか? それともどちらか一方でMVPに含めますか?

> 回答:両方サポートで構いません。

- 質問3-2: 文字コードはUTF-8のみで良いですか? Shift_JISの対応は不要ですか?

> 回答:UTF-8のみで良いです。

- 質問3-3: 重複コードがあった場合の扱いは? （警告? 自動排除? そのまま生成?）

> 回答:警告表示の上、自動排除。

- 質問3-4: 複数列CSVに対応しますか? それとも1列のコードのみですか? もし複数列対応する場合、ラベルに使う列を選択可能にしますか?

> 回答:MVPでは1列のみ。

---

## 4. QRコード仕様

- 質問4-1: 「サイズ指定可能」とは具体的にどの範囲ですか? デフォルト値は何pxにしますか?（例: 256px、推奨範囲128〜1024px）

> 回答:デフォルト256px、範囲128〜1024pxで良いです。

- 質問4-2: 誤り訂正レベルのデフォルトは? (L=7% / M=15% / Q=25% / H=30%) 一般的にはMが多いですが、QRライブラリの推奨値で良いですか?

> 回答:Mをデフォルトにしてください。

- 質問4-3: MVPではPNGのみで良く、SVGは将来拡張で良いですか?

> 回答:はい、MVPはPNGのみで構いません。

---

## 5. ラベル仕様

- 質問5-1: 「ラベル領域高さ: 40px」は固定ですか? それともフォントサイズに応じて自動で伸縮しますか?

> 回答:フォントサイズと折返し行数に応じて自動で伸縮してください。最小40px。

- 質問5-2: ラベルが長い場合の「折り返し」は何文字 or 何pxで折り返しますか? QR幅に合わせる想定で良いですか?

> 回答:QR幅に合わせて折り返してください。

- 質問5-3: 「コード + 任意テキスト」モードでの表示形式は? 例:
  - `GAME01 - 卓1番`（区切り文字で連結）
  - 2行表示（1行目コード、2行目テキスト）
  - テンプレート文字列（例: `{CODE}:{TEXT}`）

> 回答:テンプレート文字列方式（例: `{CODE} {TEXT}` をユーザーが指定）でお願いします。

- 質問5-4: ラベル位置の選択肢は「QRの下のみ」で良いですか? それとも上・左・右もありますか?

> 回答:MVPでは「下のみ」でOKです。

---

## 6. 出力形式

- 質問6-1: ZIP内のファイル名はコード文字列（例: `GAME01.png`）で良いですか? コードに使えない文字（`/`、`\`、`:` 等）が含まれる場合のサニタイズ方針は?

> 回答:コード名そのまま、使えない文字は `_` に置換してください。重複した場合は連番を付与。

- 質問6-2: 一覧HTMLは「画面上のプレビュー」を指していますか? それとも `index.html` としてZIPに含める必要がありますか?

> 回答:ZIPに含めてください。

- 質問6-3: 印刷レイアウトのA4は縦向きのみですか? 横向きも必要ですか?

> 回答:縦向きのみでOKです。

- 質問6-4: 印刷時の余白・QRサイズは固定値ですか? それともユーザーが調整可能ですか?

> 回答:MVPは固定値でOK。

---

## 7. UI・UX

- 質問7-1: ダークモード対応は「OSのprefers-color-schemeに追従」で良いですか? それとも画面内にトグルボタンを設置しますか?

> 回答:画面内にトグルボタンを設置してください（OS設定を初期値とする）。

- 質問7-2: 言語は日本語UIのみで良いですか? 英語対応は不要ですか?

> 回答:日本語のみで良いです。

- 質問7-3: 生成中のプログレス表示は必要ですか?（1000〜5000件の場合、数秒〜数十秒かかる可能性）

> 回答:プログレスバーまたは「X / N 件生成中」のような表示をお願いします。

---

## 8. 技術・デプロイ

- 質問8-1: パッケージマネージャは `npm` 固定で良いですか? それとも pnpm / yarn でも良いですか?

> 回答:npmで統一してください。

- 質問8-2: GitHub Pagesデプロイ用の `vite.config.ts` の `base` 設定はリポジトリ名 `/Google-form-qr-generator/` で合っていますか?

> 回答:はい、その想定で構いません。

- 質問8-3: デプロイ用のGitHub Actionsワークフローも一緒に作成しますか?（push時に自動デプロイ）

> 回答:はい、`gh-pages` ブランチへの自動デプロイを設定してください。

- 質問8-4: 既存リポジトリ `https://github.com/DRR-Materials-Dev/Google-form-qr-generator.git` には既にコードがありますか? それともゼロから作成しますか?

> 回答:空のリポジトリです。ゼロから作成してください。

- 質問8-5: ライセンスファイル（LICENSE）やREADMEは作成しますか?

> 回答:READMEのみ作成。ライセンスは不要。

---

## 9. その他

- 質問9-1: テストコード（Vitest等）は実装範囲に含めますか?

> 回答:MVPでは不要です。

- 質問9-2: ローカル実行時のポートやコマンドの希望はありますか?（デフォルト: `npm run dev` → http://localhost:5173 ）

> 回答:Vite標準のままでOKです。

- 質問9-3: 仕様書「11. AI Agent向け補足」にある「ラベルを画像へ焼き込む」要件があるため、HTML上のプレビューでもZIPでも同じ「焼き込み済みPNG」を使う方針で良いですか?

> 回答:はい、それで構いません。

---

回答が揃い次第、実装を開始します。
