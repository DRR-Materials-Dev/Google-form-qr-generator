# GoogleFormQR大量生成ツール 設計書

同フォルダ内の `仕様書.md` と親フォルダの `../質問事項.md` の回答に基づき実装した内容を整理する。

- バージョン: 1.1.0
- バージョン管理: `package.json` の `version` を Vite `define` で `__APP_VERSION__` として注入 (`src/vite-env.d.ts` で型宣言、`App.tsx` ヘッダーに表示)
- 履歴: 1.0.0 (初版) → 1.1.0 (設定の保存/読込機能を追加)
- 著作権: 防災教材開発G
- ライセンス: MIT
- リポジトリ: <https://github.com/DRR-Materials-Dev/Google-form-qr-generator>
- 公開URL (予定): <https://drr-materials-dev.github.io/Google-form-qr-generator/>

---

## 1. 概要

Googleフォーム事前入力URLの一部 (`{CODE}` プレースホルダ) を、連番またはCSV入力で大量置換し、それぞれのURLに対応するQRコード画像 (PNG) を一括生成する単一ページアプリ。すべての処理がブラウザ内で完結し、サーバー送信は発生しない。

主用途: イベント受付 / 卓番号管理 / ゲームセッション管理 / 避難所区画管理 / 来場者識別。

---

## 2. プロジェクト構成

```
GoogleFormQR大量生成ツール/                  # 親作業ディレクトリ
├─ 質問事項.md                              # 仕様確認Q&A (実装の前提)
├─ ローカル検証項目.md                       # 検証チェックリスト
├─ setup.bat / dev.bat / build.bat / preview.bat   # 起動スクリプト (Shift-JIS, CRLF)
└─ Google-form-qr-generator/                # アプリ本体 (Vite + React + TS)
    ├─ 仕様書.md                            # 元仕様書
    ├─ 設計書.md                            # 本ファイル
    ├─ README.md                            # ライセンス・公開URL等
    ├─ .github/workflows/deploy.yml         # GitHub Pages 自動デプロイ
    ├─ index.html
    ├─ vite.config.ts                       # base: '/Google-form-qr-generator/'
    ├─ tailwind.config.js                   # darkMode: 'class'
    ├─ postcss.config.js
    ├─ package.json
    └─ src/
       ├─ main.tsx                          # エントリ
       ├─ index.css                         # Tailwind base + 印刷用 .no-print
       ├─ App.tsx                           # 画面 + 状態管理 + 動的@page注入
       ├─ hooks/
       │   └─ useDarkMode.ts                # OS連動+localStorage永続化
       ├─ components/
       │   └─ ui.tsx                        # Tailwind UIプリミティブ
       └─ lib/
           ├─ types.ts                      # 型定義 + 定数
           ├─ codeGenerator.ts              # 連番/CSV → コード配列
           ├─ labelText.ts                  # URLビルド + ラベル文字列ビルド
           ├─ qrRenderer.ts                 # Canvas で QR + ラベル焼き込み
           ├─ sanitize.ts                   # ファイル名サニタイズ + 衝突回避
           ├─ zipBuilder.ts                 # ZIP + index.html 同梱
           ├─ settingsIO.ts                 # 設定のJSON保存/読込 (v1.1.0~)
           └─ pipeline.ts                   # 全体オーケストレーション
```

---

## 3. 技術スタック

| 区分 | 採用 | バージョン (主要) | 備考 |
|---|---|---|---|
| ビルド | Vite | 8.x | `npm create vite` で初期化 |
| 言語 | TypeScript | 5.x | `tsc -b` を build に組み込み |
| UI | React | 19 | StrictMode |
| スタイル | Tailwind CSS | 3.x | `darkMode: 'class'` |
| QR生成 | qrcode | latest | Canvas API へ描画 |
| ZIP生成 | jszip | latest | `generateAsync({ type: 'blob' })` |
| 配信 | GitHub Pages | — | Actions による自動デプロイ |

---

## 4. データモデル (型定義)

`src/lib/types.ts` に集約。

```ts
type GenerationMode = 'sequence' | 'csv'
type ErrorCorrectionLevel = 'L' | 'M' | 'Q' | 'H'
type LabelContentMode = 'code' | 'url' | 'text' | 'template'
type PaperSize = 'A4' | 'A5' | 'B4' | 'B5' | 'Letter' | 'Legal'

interface AppConfig {
  urlTemplate: string                  // {CODE} を含むURL雛形
  mode: GenerationMode
  sequence: { prefix; start; end; digits }
  csv: { text: string }
  qr: { size: number; errorCorrectionLevel: ErrorCorrectionLevel }
  label: {
    enabled; contentMode; customText; template;
    fontSize; fontColor; fontFamily;
  }
  print: {
    paperSize; qrScale;                // qrScale: %
    qrGapRight; qrGapBottom;           // px
    pageMarginTop/Right/Bottom/Left;   // px
    showCodeText; showUrlText;
    showCutGuideBorder;                // 切り取り用枠線 ON/OFF
  }
}

interface GeneratedItem {
  code; url; labelText;
  pngBlob: Blob; pngDataUrl: string;
  fileName: string;
}

const MAX_COUNT = 5000
const PLACEHOLDER = '{CODE}'
```

---

## 5. 主要モジュール

### 5.1 codeGenerator.ts — コード配列生成

| 関数 | 機能 | バリデーション |
|---|---|---|
| `generateFromSequence(cfg)` | 連番生成 (Prefix + 0埋め数字) | start/end整数・桁数1以上・start≤end・終了番号≤桁数最大値 (桁あふれエラー) ・件数≤5000 |
| `generateFromCsv(cfg)` | CSV/テキスト入力 | 空入力エラー / 空行スキップ / BOM除去 / 1列目のみ採用 / 重複は自動排除し件数を返却 / 件数≤5000 |

エラーは `CodeGenerationError` で投げ、UIで日本語メッセージとして表示。

### 5.2 labelText.ts — 文字列ビルダ

- `buildUrl(template, code)`: `{CODE}` を置換。
- `buildLabelText(label, code, url)`: ラベル表示モードに応じて文字列を組み立てる。
  - `code` / `url` / `text` (任意テキスト) / `template` (`{CODE}` `{URL}` `{TEXT}` 展開)

### 5.3 qrRenderer.ts — QR画像生成 (核心)

仕様書 §11 の最重要要件「ラベルを画像へ焼き込む」を担う。

処理フロー:
1. `QRCode.toCanvas` で QR本体を一時Canvasに描画 (`qr.size` px, 誤り訂正レベル設定)。
2. ラベル有効時のみ:
   - 計測用Canvasで `wrapText` を実行し、QR幅以内に収まるよう文字を折返し
   - 行高 = `fontSize × 1.3`、ラベル領域高さ = 行数 × 行高 + 上下パディング、最小 **40px**
3. 出力Canvas (`width = qr.size`, `height = qr.size + labelHeight`) を生成し、以下の順で描画:
   - 背景 (白)
   - QRコード
   - ラベル文字 (中央揃え、フォントサイズ・色・フォント反映)
4. PNG Blob と DataURL を返却。

### 5.4 sanitize.ts — ファイル名処理

- `sanitizeFileNameStem`: `\\ / : * ? " < > |` および制御文字を `_` に置換。先頭の `.` も置換。
- `uniquifyFileNames`: 同名衝突時に `_2`, `_3` ... をサフィックス付与し、サフィックス付きの結果がさらに衝突する場合もインクリメントして回避。

### 5.5 zipBuilder.ts — ZIP出力

- 各 `GeneratedItem.pngBlob` を `<fileName>` で格納
- `index.html` を ZIP 内に同梱: グリッド表示の一覧、各画像へのリンク、個別ダウンロードボタンを含む静的HTML

### 5.6 settingsIO.ts — 設定の保存 / 読込 (v1.1.0~)

Section 1〜5 の全設定 (`AppConfig`) を JSON ファイルとして保存・復元するモジュール。

- `serializeConfig(cfg, appVersion?)`: `$schema` / `version` / `savedAt` / `appVersion` / `config` を含むラッパーJSON を整形して文字列化
- `parseConfig(text, defaults)`: パース時に以下を実施
  - JSON パース失敗 → `SettingsParseError`
  - `$schema !== "qrsettings"` → `SettingsParseError` で拒否
  - `config` 配下の各フィールドを型・enum 検査し、不正/欠落は `defaults` で補完
- 定数:
  - `SETTINGS_SCHEMA = 'qrsettings'`
  - `SETTINGS_SCHEMA_VERSION = 1`
  - `SETTINGS_EXTENSION = 'qrsettings.json'`

JSON フォーマット:

```json
{
  "$schema": "qrsettings",
  "version": 1,
  "savedAt": "2026-05-12T01:30:00.000Z",
  "appVersion": "1.1.0",
  "config": {
    "urlTemplate": "...",
    "mode": "sequence",
    "sequence": { "prefix": "GAME", "start": 1, "end": 50, "digits": 2 },
    "csv": { "text": "..." },
    "qr": { "size": 256, "errorCorrectionLevel": "M" },
    "label": { ... },
    "print": { ... }
  }
}
```

### 5.7 pipeline.ts — 全体オーケストレーション

- `validateUrlTemplate`: `{CODE}` 含有 + URL妥当性 (`new URL` でparse) チェック
- `prepareCodes`: 上記+選択モードでコード配列を返却 (重複情報含む)
- `runPipeline`: コード配列→`renderQrWithLabel`を直列実行、32件ごとに `setTimeout(0)` を挟んでUIスレッドを解放、`onProgress` 通知。

`PipelineError` を投げる契約。UI側で `catch` してアラート表示。

---

## 6. UI設計

### 6.1 画面構造

| 領域 | 内容 |
|---|---|
| ヘッダー | アプリ名 + バージョンバッジ / ダークモードトグル (`.no-print`) |
| 設定IO | 設定ファイル名入力 + 「設定を保存」「設定を読込」「初期値に戻す」 (`.no-print`、v1.1.0~) |
| メイン (設定エリア) | Section 1〜5 を2カラムグリッドで配置 (`.no-print`) |
| 実行コントロール | 生成ボタン / ZIPダウンロード / 印刷ボタン (`.no-print`) |
| 状態表示 | エラー (赤) / 警告 (黄) / 進捗 (青、X/N + バー) (`.no-print`) |
| 結果一覧 | `<ul class="print-grid grid …">` 内に各QRカード |
| フッター | コピーライト / GitHubリンク (`.no-print`) |

### 6.2 設定セクション

**設定の保存 / 読込** (v1.1.0~) — Section 1〜5 全体を `.qrsettings.json` として保存/復元 (本書 §5.6)。ファイル名入力欄 + 固定サフィックス `.qrsettings.json` + 3ボタン (保存 / 読込 / 初期値に戻す)。ファイル選択ダイアログは `accept=".qrsettings.json"` でフィルタ。

1. **URLテンプレート** — 1行入力 + 先頭コードでのプレビュー表示
2. **コード生成** — ラジオで連番/CSV切替。連番モード: Prefix/開始/終了/桁数。CSVモード: テキストエリア + ファイル読込ボタン
3. **QR設定** — サイズ (128〜1024) / 誤り訂正レベル (L/M/Q/H、初期M)
4. **ラベル設定** — 有効/無効、表示内容モード、任意テキスト、テンプレート、フォントサイズ・色・フォント
5. **印刷レイアウト** — 後述

### 6.3 ダークモード

- `useDarkMode` フックが OS の `prefers-color-scheme` を初期値とし、トグル状態を `localStorage` に保存
- 切替は `html` 要素に `class="dark"` を付け外し → Tailwind の `dark:` バリアントが反映

### 6.4 印刷の遮断 / 解放

- 印刷で隠したい全ての要素に `.no-print` クラスを付与し、`@media print { .no-print { display: none !important; } }` で除外
- 結果一覧のみが印刷対象

---

## 7. 印刷レイアウトシステム (詳細)

### 7.1 設定値

| 項目 | 範囲 | 初期値 |
|---|---|---|
| 用紙サイズ | A4 / A5 / B4 / B5 / Letter / Legal | A4 |
| 向き | 縦向き固定 (将来拡張) | portrait |
| QR縮尺 | 10〜300% | 100% |
| QR間 右マージン | 0〜200px | 8px |
| QR間 下マージン | 0〜200px | 8px |
| 用紙余白 上/右/下/左 | 0〜500px (各) | 各40px |
| 印刷時コード文字 | ON/OFF | ON |
| 印刷時URL | ON/OFF | OFF |
| 切り取り用枠線 | ON/OFF | ON (薄い灰色 `#d1d5db`、1px) |

### 7.2 動的CSS注入

`App.tsx` 内で設定値から CSS テキストを組み立て、`<style>{printStyle}</style>` として埋め込む。設定変更時はReactの再描画で `<style>` ノードの内容も差し替わる。

```css
@page {
  size: <PaperSize> portrait;
  margin: <Top>px <Right>px <Bottom>px <Left>px;
}
@media print {
  html, body { background: #fff !important; color: #000 !important; }
  .print-grid {
    display: grid !important;
    grid-template-columns: repeat(auto-fill, <printedQrWidth>px) !important;
    column-gap: <qrGapRight>px !important;
    row-gap: <qrGapBottom>px !important;
    justify-content: start !important;
    padding: 0 !important; margin: 0 !important; list-style: none !important;
  }
  .print-grid > li {
    width: <printedQrWidth>px !important;
    break-inside: avoid; page-break-inside: avoid;
    background: #fff !important; border: none !important;
    padding: 0 !important; text-align: center; color: #000 !important;
  }
  .print-grid img {
    width: <printedQrWidth>px !important; height: auto !important;
  }
  /* showCodeText / showUrlText が OFF の場合のみ */
  .print-grid .code { display: none !important; }
  .print-grid .url { display: none !important; }
}
```

`printedQrWidth = qr.size × qrScale / 100`

### 7.3 設計の意図

- **列数を手動指定しない**: 用紙幅 / (QR幅 + 右マージン) で自動的に列数が決まるため、ユーザーは「列数」ではなく「実寸 (QR縮尺) と隙間」のメンタルモデルで操作できる
- **`break-inside: avoid`**: 改ページ時にQRが分断されないことを保証
- **ダークモード強制解除**: 印刷では常に白背景・黒文字
- **`@page margin` は px で指定**: 仕様要望 (px指定) に従う。ブラウザ側で 96dpi 基準で物理寸法に変換される

### 7.4 制約と既知の挙動

- ブラウザの「ヘッダー・フッター」(URLや日付) は `@page` で消せないため、ユーザーが印刷ダイアログ側で OFF にする必要がある
- 余白を 0 にしても、プリンタの物理印刷不可領域は残る
- 用紙の向きは縦固定 (横向きは将来拡張)

---

## 8. 処理フロー (生成 → 出力)

```
[1] ユーザーが「QRコード生成」を押下
    │
[2] prepareCodes(cfg)
    ├── validateUrlTemplate
    ├── 連番モード → generateFromSequence
    └── CSVモード  → generateFromCsv (重複自動排除)
    │
[3] 重複があれば警告メッセージを表示
    │
[4] runPipeline
    ├── ファイル名を一括サニタイズ・衝突回避
    └── 各コードについて renderQrWithLabel
        ├── QRCode.toCanvas
        ├── ラベル折返し計算
        ├── 出力Canvas に背景 → QR → ラベル を順次描画
        └── PNG Blob / DataURL を生成
    └── 32件ごとに setTimeout(0) で UI スレッド解放、進捗を通知
    │
[5] items[] を state に保存 → 画面プレビューが <img src=dataUrl> で表示
    │
[6a] ZIPダウンロード: buildZip(items) → 各PNG + index.html を含む ZIP
[6b] 印刷: window.print() → 動的@pageに従い印刷
[6c] 個別ダウンロード: 各カードの「ダウンロード」リンクで単体PNG保存
```

### 8.1 設定の保存 / 読込フロー (v1.1.0~)

```text
[保存] ユーザーがファイル名を入力 → 「設定を保存」押下
    │
    ├── serializeConfig(cfg, __APP_VERSION__) で JSON文字列を生成
    ├── ファイル名を sanitizeFileNameStem で安全化
    └── triggerDownload(<stem>.qrsettings.json)

[読込] 「設定を読込」押下 → ファイル選択 (accept=".qrsettings.json")
    │
    ├── file.text() で UTF-8 読込 (BOM除去)
    ├── parseConfig(text, DEFAULT_CONFIG)
    │     ├── JSON.parse 失敗 → SettingsParseError
    │     ├── $schema 不一致 → SettingsParseError で拒否
    │     └── 各フィールドを型・enum検査、不正/欠落は DEFAULT_CONFIG で補完
    ├── setCfg(loaded) で全Sectionに即時反映
    └── 入力欄にファイル名 (拡張子除く) を自動セット

[初期化] 「初期値に戻す」押下 → setCfg(DEFAULT_CONFIG)
```

---

## 9. ビルド・デプロイ

### 9.1 起動スクリプト (Windows)

親フォルダの `.bat` 群 (Shift-JIS / CRLF):

- `setup.bat`: Node/npm 確認 + `npm install`
- `dev.bat`: `npm run dev` (http://localhost:5173)
- `build.bat`: `npm run build` → `dist/`
- `preview.bat`: `npm run preview` (http://localhost:4173)

### 9.2 GitHub Pages デプロイ

`.github/workflows/deploy.yml` が `main` への push をトリガに以下を実行:

1. Node 20 セットアップ
2. `npm ci`
3. `npm run build`
4. `dist/` を `actions/upload-pages-artifact` → `actions/deploy-pages`

`vite.config.ts` の `base: '/Google-form-qr-generator/'` により、ビルド出力のアセットURLが `/Google-form-qr-generator/assets/...` になる。

---

## 10. 仕様書とのトレーサビリティ (主要要件のみ)

| 仕様書セクション | 実装 |
|---|---|
| §3.1 URLテンプレート | `App.tsx` 入力欄 + `pipeline.validateUrlTemplate` |
| §3.2 A. 連番生成 | `codeGenerator.generateFromSequence` |
| §3.2 B. CSV入力 | `codeGenerator.generateFromCsv` + テキストエリア + ファイル読込 |
| §4 QR仕様 (PNG/サイズ/誤り訂正) | `qrRenderer.renderQrWithLabel` |
| §4.4〜 ラベル仕様 (画像焼き込み) | `qrRenderer` (出力Canvasへ描画) |
| §5.1 ZIPダウンロード | `zipBuilder.buildZip` |
| §5.2 一覧HTML | 画面プレビュー + ZIP内 `index.html` |
| §5.3 印刷レイアウト | 動的`@page` + `.print-grid` (本書 §7) |
| §6 UI仕様 | `App.tsx` セクション1〜5 |
| §7 技術仕様 | React + TS + Vite + Tailwind |
| §8 デプロイ / 1000件対応 | Actions ワークフロー + 32件ごとのスレッド解放 |
| §9.1 URLチェック | `validateUrlTemplate` |
| §9.2 件数制限 5000 | `MAX_COUNT` |
| §10 描画順序 (背景→QR→ラベル) | `qrRenderer` 内で固定順 |
| §11 ラベル焼き込み厳守 | プレビューもZIPも同一PNGを参照 |
| §13 実装方針 | SPA / npm install のみ / 日本語UI / ダークモード / エラー表示 |
| (追加) 設定の保存/読込 | `settingsIO.serializeConfig` / `parseConfig` + `App.tsx` の設定IOセクション (v1.1.0) |

---

## 11. 将来拡張 (未実装)

仕様書 §12 のうち、MVP では以下を後回し:

- PDF出力
- SVG出力
- バーコード
- NFCタグ書き込み
- 横向き印刷
- 用紙余白の物理単位 (mm/in) 指定
- 名札風レイアウト
- Google Spreadsheet 連携
- 連番ステップ (1以外) / Suffix
- 複数列CSV (ラベル列指定)
- Shift_JIS CSV
- 多言語UI (英語等)
- テストコード (Vitest)

---

## 12. ライセンス情報

- 本ツール本体: **MIT License** (Copyright (c) 2026 防災教材開発G)
- 使用ライブラリのライセンス一覧は README.md を参照
