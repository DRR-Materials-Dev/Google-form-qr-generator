# GoogleFormQR大量生成ツール ローカル検証項目

デプロイ前にローカル環境で実施する検証項目です。各項目を確認し、問題がなければチェックを入れてください。

実施環境例:

```text

OS: Windows 11

Node: 20.x 以上

ブラウザ: Chrome 最新 / Edge 最新 / Firefox 最新

```

---

## 1. セットアップ・起動

- [X] `npm install` がエラーなく完了する
- [X] `npm run dev` で開発サーバーが起動する
- [X] http://localhost:5173 にブラウザでアクセスできる
- [X] 初回ロード時にコンソールエラーが出ていない
- [X] `npm run build` が成功する
- [X] `npm run preview` でビルド成果物が動作する
- [X] ビルド後の `dist/` の総サイズが妥当（数MB以内）

---

## 2. URLテンプレート入力

- [X] URL入力欄にテンプレート文字列が入力できる
- [X] `{CODE}` が含まれないURLを入力して生成すると、明確なエラーメッセージが表示される
- [X] `{CODE}` を含む正常なURLでバリデーションが通過する
- [X] 半角スペース・改行など混入時に適切にトリムまたはエラー表示される
- [X] 長いURL（500文字程度）でもUIが崩れない
- [X] 日本語を含むURLでも正常に処理される

---

## 3. 連番生成モード

- [X] Prefix / 開始番号 / 終了番号 / 桁数 を入力できる
- [X] `Prefix=GAME, 開始=1, 終了=50, 桁数=2` で `GAME01`〜`GAME50` が生成される
- [X] 桁数=2、終了=150 のように桁あふれが発生する組み合わせでバリデーションエラーが表示される
- [X] 開始番号 > 終了番号 の場合にエラーになる
- [X] 0 件になる入力（開始=終了+1 など）でエラー表示される
- [X] 開始番号=0、桁数=3 で `GAME000` が生成される（境界値）
- [X] Prefix が空でも数字のみのコードとして生成できる
- [X] 件数が5000を超えるとエラー表示される
- [X] 件数がちょうど5000で正常生成できる

---

## 4. CSV入力モード

- [X] テキストエリアに改行区切りでコードを入力して生成できる
- [X] CSVファイル（.csv / .txt）をアップロードして読み込める
- [X] UTF-8（BOMなし）のファイルが正しく読み込める
- [X] UTF-8（BOM付き）のファイルでも文字化けしない
- [X] 空行が自動的に無視される
- [X] 重複コードがある場合、警告メッセージが表示される
- [X] 重複コードは自動排除され、ユニークなコードのみQR生成される
- [X] 排除された重複の件数が画面に表示される
- [X] 1列のCSVのみが処理対象になっている（複数列は1列目のみ使用 or エラー、仕様通り）
- [X] 5000件超のCSVでエラー表示される
- [X] 空のCSV/テキストエリアでエラー表示される

---

## 5. QRコード生成

- [X] QRサイズ デフォルト256pxで生成される
- [X] サイズを128pxに変更しても生成できる
- [X] サイズを1024pxに変更しても生成できる
- [X] サイズ範囲外（127px以下、1025px以上）はバリデーションエラーになる
- [X] 誤り訂正レベル L / M / Q / H が選択できる
- [X] デフォルトの誤り訂正レベルが M になっている
- [X] スマホのカメラで生成されたQRコードを読み取り、正しいURLに遷移する
- [X] Google Form 事前入力URLとして読み取り、フォームが正しい値で開く
- [X] 出力PNGの解像度が指定サイズと一致している
- [X] QRコードが鮮明（ぼやけ・ジャギーがない）

---

## 6. ラベル仕様

- [X] 「ラベルを表示する」チェックボックスのON/OFFで挙動が切り替わる
- [X] ラベルOFF時はQRコードのみのPNGが出力される
- [X] ラベル表示内容「コードのみ」が正しく表示される
- [X] ラベル表示内容「URL」が正しく表示される
- [X] ラベル表示内容「任意テキスト」が正しく表示される
- [X] ラベル表示内容「コード+任意テキスト（テンプレート）」で `{CODE} {TEXT}` が展開される
- [X] テンプレート内の `{CODE}``{TEXT}` 以外のプレースホルダは展開されない（または無視される）
- [X] フォントサイズを変更すると画像内のラベルサイズが変化する
- [X] フォントサイズが小さいときラベル領域高さが最小40pxを維持する
- [X] 長いラベルがQR幅で折り返される
- [X] 折返し行数が増えるとラベル領域が自動で高くなる
- [X] ラベルがQR本体に被らない
- [X] PNGダウンロードした画像にラベルが焼き込まれている（HTMLテキストではない）

---

## 7. 出力（ZIPダウンロード）

- [X] 「QRコード生成」ボタンを押すと進捗表示（X / N 件）が出る
- [X] 生成完了後にZIPダウンロードボタンが有効になる
- [X] ZIPファイルをダウンロードできる
- [X] ZIP内に `<コード名>.png` がコードの件数分含まれている
- [X] ZIP内に `index.html` が含まれている
- [X] `index.html` をブラウザで開くと一覧表示される
- [X] `index.html` 内の画像参照が正しく解決される
- [X] コード名に `/`, `\\`, `:`, `*`, `?`, `"`, `<`, `>`, `|` 等を含むときファイル名が `_` に置換される
- [X] サニタイズ後にファイル名が重複する場合、連番が付与されて衝突しない
- [X] 各PNG画像が単独で開ける（破損していない）

---

## 8. 画面プレビュー / 一覧HTML

- [X] 生成後に画面内に一覧（QR画像 / コード名 / URL / 個別DLボタン）が表示される
- [X] 個別ダウンロードボタンで該当コードのPNGがダウンロードできる
- [X] プレビュー画像がZIP内のPNGと同一（焼き込み済み）である
- [X] 件数が多いとき（例: 1000件）でも一覧表示で画面がフリーズしない（仮想スクロール等の最低限の対策）

---

## 9. 印刷レイアウト

### 9.1 設定UI

- [X] 「5. 印刷レイアウト」セクションが設定エリアに表示される
- [X] 用紙サイズが A4 / A5 / B4 / B5 / Letter / Legal から選択できる
- [X] QR縮尺 (%) の初期値が100で、10〜300の範囲で入力できる
- [X] QR縮尺の範囲外 (9以下、301以上) は最寄りの境界値に丸められる、または入力できない
- [X] 「印刷時QR幅 (px)」が QRサイズ × 縮尺 で自動計算されて表示される
- [X] QR間 右マージン / 下マージン が px単位で 0〜200 で指定できる
- [X] 用紙余白 上 / 右 / 下 / 左 が px単位で 0〜500 で指定できる
- [X] 「印刷時にコード文字を表示」チェックボックスが切り替えられる
- [X] 「印刷時にURLを表示」チェックボックスが切り替えられる
- [X] 「切り取り用の枠線を表示」チェックボックスが切り替えられる (初期: ON)

### 9.2 印刷プレビューでの反映 (Ctrl+P)

- [X] 用紙サイズ A4 を選択すると、印刷プレビューの用紙サイズが A4 になる
- [X] 用紙サイズ B5 を選択すると、印刷プレビューが B5 になる
- [X] 用紙サイズ Letter を選択すると、印刷プレビューが Letter になる
- [X] 用紙が縦向き (portrait) で固定されている
- [X] QR縮尺=50% で QRが半分のサイズで印刷される
- [X] QR縮尺=200% で QRが2倍のサイズで印刷される
- [X] QR縮尺の変更に応じて1ページに収まる枚数が自動で増減する
- [X] QR間 右マージン (px) がQR画像の横間隔として反映される
- [X] QR間 下マージン (px) がQR画像の縦間隔として反映される
- [X] 用紙余白 上/右/下/左 が印刷プレビューの余白として反映される
- [X] 「コード文字を表示」OFF で印刷プレビューにコード文字が表示されない
- [X] 「URLを表示」ON で印刷プレビューにURLが表示される
- [X] 「切り取り用の枠線を表示」ON で各QRの周囲に薄い灰色の枠が表示される
- [X] 「切り取り用の枠線を表示」OFF で枠線が表示されない
- [X] 枠線ありで印刷した際にカッターやハサミで切り取りやすい (実際の印刷物で確認)

### 9.3 レイアウト品質

- [X] 用紙幅に応じてQRが自動的に折り返され、余白がはみ出さない
- [X] 改ページが適切に挿入される (QRが途中で分断されない)
- [X] ダークモード状態でも印刷時の背景は白、文字は黒になる
- [X] 印刷時にヘッダー / フッター / 操作UI / 設定エリアが印刷に含まれない
- [X] 印刷時にラベル文字列 (画像焼き込み) が読める
- [X] 余白を 0px にしても用紙端ぴったりにQRが配置される (ブラウザ側の最低余白による制約を除く)

---

## 10. UI / UX

- [X] 日本語UIで表示されている
- [X] エラーメッセージが日本語で分かりやすい
- [X] ダークモードトグルで明色/暗色が切り替わる
- [X] OS設定がダークの場合、初期表示がダークモードになる
- [X] OS設定がライトの場合、初期表示がライトモードになる
- [X] ダークモードでも文字とコントラストが十分（可読性OK）
- [X] モバイル幅（375px）でレイアウトが崩れない
- [X] タブレット幅（768px）でレイアウトが崩れない
- [X] デスクトップ幅（1280px以上）でレイアウトが崩れない
- [X] フォーカスリングが視認可能（キーボード操作可）
- [X] フォームの必須項目が分かるラベリングになっている

---

## 11. パフォーマンス

- [X] 100件生成が数秒以内に完了する
- [X] 1000件生成が完了する（仕様の最低要件）
- [X] 1000件生成中もUIが完全フリーズせず進捗が更新される
- [X] 5000件生成が完了する（上限値）
- [X] 5000件生成時にメモリ不足やブラウザクラッシュが発生しない
- [X] 大量生成後にZIPダウンロードが完了する
- [X] 大量生成後、ページ遷移なしで再生成しても問題ない（メモリリークなし）

---

## 12. エラー処理・エッジケース

- [X] 入力途中で生成ボタンを押した場合、未入力項目が明示される
- [X] 連番モードとCSVモードを切り替えても入力値が混在しない
- [X] 生成中に再度生成ボタンを押せない（二重実行されない）
- [X] ネットワーク切断状態でも動作する（完全クライアントサイド）
- [X] ページリロード後も問題なく再使用できる
- [X] コンソールにエラー・警告が出ていない（React/TypeScriptの警告含む）

---

## 13. ビルド成果物の確認

- [X] `dist/index.html` の参照パスが GitHub Pages の base パスに対応している
- [X] `dist/` をローカルの静的サーバー（例: `npx serve dist`）で配信して動作する
- [X] base パスの設定確認: ビルド済みHTMLでアセットURLが `/Google-form-qr-generator/...` になっている
- [X] 外部CDN依存がなく、`dist/` 配下のみで完結している

---

## 14. ドキュメント

- [X] README.md に概要 / 起動方法 / ビルド方法が記載されている
- [X] README.md にデプロイURL（後で更新）の欄がある
- [X] README.md にスクリーンショットまたは使い方の説明がある

---

## 15. 設定の保存 / 読込 (Ver 1.1.0 以降)

### 15.1 UI

- [X] 設定エリアの最上部に「設定の保存 / 読込」セクションが表示される
- [X] ファイル名入力欄の右側に `.qrsettings.json` の固定サフィックスが表示される
- [X] ファイル名入力欄の初期値が `qr-settings` になっている
- [X] 「設定を保存」「設定を読込」「初期値に戻す」の3ボタンが表示される
- [X] 操作後に成功/エラーのメッセージが表示される

### 15.2 保存機能

- [X] 「設定を保存」を押すと `<ファイル名>.qrsettings.json` がダウンロードされる
- [X] ダウンロードしたファイルが有効な JSON である
- [X] JSON に `$schema` `version` `savedAt` `appVersion` `config` の各フィールドが含まれる
- [X] `config` 配下に Section 1〜5 の全設定値が含まれている (urlTemplate / mode / sequence / csv / qr / label / print)
- [X] ファイル名に使えない文字 (`/\\:*?"<>|`) を含めると `_` に置換される
- [X] ファイル名を空にした場合、`qr-settings.qrsettings.json` として保存される

### 15.3 読込機能

- [X] ファイル選択ダイアログで `.qrsettings.json` を選んで読込できる
- [X] Chrome/Edge ではファイルピッカーが `.qrsettings.json` でフィルタされる
- [X] 読み込んだファイル名 (拡張子を除いた部分) が入力欄に自動セットされる
- [X] 保存→別環境で読込で Section 1〜5 の全設定が完全に復元される
- [X] スキーマ不一致 (例: `$schema` フィールドが無いJSON) は明確なエラーで拒否される
- [X] JSON として不正なファイルは「JSONとして解析できませんでした」とエラー表示される
- [X] 一部フィールドが欠落した JSON でも、欠落分は初期値で補完して読込が成功する
- [X] 不正な enum 値 (例: `paperSize: "INVALID"`) はその項目だけ初期値で補完される
- [X] UTF-8 BOM 付きの設定ファイルでも正しく読み込める

### 15.4 初期値に戻す

- [X] 「初期値に戻す」を押すと Section 1〜5 の全設定が初期値に戻る
- [X] 「初期値に戻す」操作は確認ダイアログなしで即時実行されるが、保存していなければ元に戻せないため取り扱いに注意 (UI上に注意書きは現状なし)

### 15.5 統合シナリオ

- [X] 「設定を保存」→ ブラウザリロード → 「設定を読込」で復元されることを確認
- [X] 複数の設定ファイル (例: `event-A.qrsettings.json` `event-B.qrsettings.json`) を切り替え使用できる
- [X] バージョン情報 (`appVersion`) が保存ファイルに記録されている

---

## 16. {CODE} 不使用 / 単一QR生成 (Ver 1.2.0 以降)

### 16.1 UI

- [X] Section 1 内に「URLに `{CODE}` プレースホルダを使い、コードごとに置換する」チェックボックスが表示される
- [X] チェックボックスの初期値が ON (チェック済み) になっている
- [X] チェックボックスを OFF にすると、Section 2「コード生成」全体が非表示になる
- [X] チェックボックスを OFF にすると、Section 1 のラベルが「URLテンプレート」から「URL」に変わる
- [X] チェックボックスを OFF にすると、ヒント文言が「このURLでQRコードを1枚だけ生成します。」に変わる
- [X] チェックボックスを OFF にすると、入力欄のプレースホルダが `https://example.com/` 系に変わる
- [X] チェックボックスを OFF にすると、生成ボタン横の表示が「上限: 5000件」から「単一QR生成モード」に変わる
- [X] チェックボックスを OFF にすると、プレビューが URL をそのまま表示する (置換なし)
- [X] チェックボックスを OFF/ON で切り替えても URL 入力値が保持される

### 16.2 生成動作 (useCode = OFF)

- [X] `{CODE}` を含まない通常のURL (例: `https://example.com/`) を入力して「QRコード生成」を押すと、QR が 1枚だけ生成される
- [X] 生成された QR のURL が入力値そのまま (置換なし) になっている
- [X] スマホで読み取ると入力した URL に遷移する
- [X] ZIPダウンロードしたファイル内に `qr.png` が 1ファイルだけ含まれている
- [X] 画面プレビューにも QR が 1枚だけ表示される
- [X] 個別ダウンロードボタンでもPNGがダウンロードできる
- [X] ラベルを有効にしている場合、ラベル文字 (URL モード / 任意テキスト / テンプレート) が画像に焼き込まれる
- [X] ラベル表示モード「コードのみ」を選んでいる場合、コードが空のためラベル領域が描画されない (空ラベルは描画スキップ)
- [X] URL を空にして生成すると「URLを入力してください。」エラーが表示される
- [X] URL が不正な形式 (例: `not-a-url`) の場合「URLが有効な形式ではありません。」エラーが表示される

### 16.3 生成動作 (useCode = ON, 既存挙動の維持)

- [X] チェックON時の動作が Ver 1.1.0 までと変わらない (連番生成 / CSV生成いずれも従来通り)
- [X] チェックON時に URL に `{CODE}` が無いと「URLに {CODE} が含まれていません。」エラーになる
- [X] チェックON時の上限 5000件 / 重複自動排除などのバリデーションは従来通り動作する

### 16.4 設定の保存 / 読込との連動

- [X] チェックON で保存した `.qrsettings.json` を読み込むとチェックがONに復元される
- [X] チェックOFF で保存した `.qrsettings.json` を読み込むとチェックがOFFに復元される
- [X] 古い (Ver 1.1.0 で保存され `useCode` フィールドが無い) `.qrsettings.json` を読み込むと `useCode = true` で補完される (後方互換)

### 16.5 印刷レイアウト

- [X] useCode = OFF で生成した単一QRも印刷レイアウト (用紙サイズ / QR縮尺 / マージン / 枠線) が反映される
- [X] useCode = OFF + 印刷時にURL/コード文字表示ON時、URL は表示され、コードは空のため非表示

### 16.6 印刷時のUI

- [X] バージョンバッジが `Ver 1.2.0` と表示される

---

完了後、未チェック項目があれば修正対応 → 全項目チェック後にデプロイへ進みます。
